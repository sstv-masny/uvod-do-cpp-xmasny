
#include <iostream>
int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    long long y;
    if (!(std::cin >> y)) return 0;
    // TODO: implement leap rule and print YES/NO
    return 0;
}
